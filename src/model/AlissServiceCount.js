/**
 * MRD API
 * API for retrieving Master Reference Data
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AlissServiceCount model module.
 * @module model/AlissServiceCount
 * @version 1.0.0
 */
class AlissServiceCount {
    /**
     * Constructs a new <code>AlissServiceCount</code>.
     * @alias module:model/AlissServiceCount
     * @param count {Number} 
     */
    constructor(count) { 
        
        AlissServiceCount.initialize(this, count);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, count) { 
        obj['count'] = count;
    }

    /**
     * Constructs a <code>AlissServiceCount</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AlissServiceCount} obj Optional instance to populate.
     * @return {module:model/AlissServiceCount} The populated <code>AlissServiceCount</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AlissServiceCount();

            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AlissServiceCount</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AlissServiceCount</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AlissServiceCount.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }

        return true;
    }


}

AlissServiceCount.RequiredProperties = ["count"];

/**
 * @member {Number} count
 */
AlissServiceCount.prototype['count'] = undefined;






export default AlissServiceCount;

