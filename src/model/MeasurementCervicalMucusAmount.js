/**
 * MRD API
 * API for retrieving Master Reference Data
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The MeasurementCervicalMucusAmount model module.
 * @module model/MeasurementCervicalMucusAmount
 * @version 1.0.0
 */
class MeasurementCervicalMucusAmount {
    /**
     * Constructs a new <code>MeasurementCervicalMucusAmount</code>.
     * Cervical mucus amount lookup record.
     * @alias module:model/MeasurementCervicalMucusAmount
     * @param id {String} 
     * @param cervicalMucusAmountId {String} 
     * @param description {String} 
     */
    constructor(id, cervicalMucusAmountId, description) { 
        
        MeasurementCervicalMucusAmount.initialize(this, id, cervicalMucusAmountId, description);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, cervicalMucusAmountId, description) { 
        obj['id'] = id;
        obj['cervical_mucus_amount_id'] = cervicalMucusAmountId;
        obj['description'] = description;
    }

    /**
     * Constructs a <code>MeasurementCervicalMucusAmount</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/MeasurementCervicalMucusAmount} obj Optional instance to populate.
     * @return {module:model/MeasurementCervicalMucusAmount} The populated <code>MeasurementCervicalMucusAmount</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new MeasurementCervicalMucusAmount();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('cervical_mucus_amount_id')) {
                obj['cervical_mucus_amount_id'] = ApiClient.convertToType(data['cervical_mucus_amount_id'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>MeasurementCervicalMucusAmount</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>MeasurementCervicalMucusAmount</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of MeasurementCervicalMucusAmount.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['cervical_mucus_amount_id'] && !(typeof data['cervical_mucus_amount_id'] === 'string' || data['cervical_mucus_amount_id'] instanceof String)) {
            throw new Error("Expected the field `cervical_mucus_amount_id` to be a primitive type in the JSON string but got " + data['cervical_mucus_amount_id']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }

        return true;
    }


}

MeasurementCervicalMucusAmount.RequiredProperties = ["id", "cervical_mucus_amount_id", "description"];

/**
 * @member {String} id
 */
MeasurementCervicalMucusAmount.prototype['id'] = undefined;

/**
 * @member {String} cervical_mucus_amount_id
 */
MeasurementCervicalMucusAmount.prototype['cervical_mucus_amount_id'] = undefined;

/**
 * @member {String} description
 */
MeasurementCervicalMucusAmount.prototype['description'] = undefined;






export default MeasurementCervicalMucusAmount;

