/**
 * MRD API
 * API for retrieving Master Reference Data
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The SearchResultItem model module.
 * @module model/SearchResultItem
 * @version 1.0.0
 */
class SearchResultItem {
    /**
     * Constructs a new <code>SearchResultItem</code>.
     * Indexed MRD page matching the supplied search filters.
     * @alias module:model/SearchResultItem
     * @param keyword {String} Indexed keyword associated with the matching page.
     * @param name {String} Name or heading of the matching MRD page.
     * @param description {String} Description of the matching page.
     * @param url {String} Absolute MRD API URL for the matching page. The model prefixes the stored route with the configured MRD host name.
     * @param scope {String} MRD service scope containing the matching result.
     */
    constructor(keyword, name, description, url, scope) { 
        
        SearchResultItem.initialize(this, keyword, name, description, url, scope);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, keyword, name, description, url, scope) { 
        obj['keyword'] = keyword;
        obj['name'] = name;
        obj['description'] = description;
        obj['url'] = url;
        obj['scope'] = scope;
    }

    /**
     * Constructs a <code>SearchResultItem</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SearchResultItem} obj Optional instance to populate.
     * @return {module:model/SearchResultItem} The populated <code>SearchResultItem</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SearchResultItem();

            if (data.hasOwnProperty('keyword')) {
                obj['keyword'] = ApiClient.convertToType(data['keyword'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('url')) {
                obj['url'] = ApiClient.convertToType(data['url'], 'String');
            }
            if (data.hasOwnProperty('scope')) {
                obj['scope'] = ApiClient.convertToType(data['scope'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SearchResultItem</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SearchResultItem</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SearchResultItem.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['keyword'] && !(typeof data['keyword'] === 'string' || data['keyword'] instanceof String)) {
            throw new Error("Expected the field `keyword` to be a primitive type in the JSON string but got " + data['keyword']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['url'] && !(typeof data['url'] === 'string' || data['url'] instanceof String)) {
            throw new Error("Expected the field `url` to be a primitive type in the JSON string but got " + data['url']);
        }
        // ensure the json data is a string
        if (data['scope'] && !(typeof data['scope'] === 'string' || data['scope'] instanceof String)) {
            throw new Error("Expected the field `scope` to be a primitive type in the JSON string but got " + data['scope']);
        }

        return true;
    }


}

SearchResultItem.RequiredProperties = ["keyword", "name", "description", "url", "scope"];

/**
 * Indexed keyword associated with the matching page.
 * @member {String} keyword
 */
SearchResultItem.prototype['keyword'] = undefined;

/**
 * Name or heading of the matching MRD page.
 * @member {String} name
 */
SearchResultItem.prototype['name'] = undefined;

/**
 * Description of the matching page.
 * @member {String} description
 */
SearchResultItem.prototype['description'] = undefined;

/**
 * Absolute MRD API URL for the matching page. The model prefixes the stored route with the configured MRD host name.
 * @member {String} url
 */
SearchResultItem.prototype['url'] = undefined;

/**
 * MRD service scope containing the matching result.
 * @member {String} scope
 */
SearchResultItem.prototype['scope'] = undefined;






export default SearchResultItem;

