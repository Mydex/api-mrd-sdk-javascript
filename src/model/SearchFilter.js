/**
 * MRD API
 * API for retrieving Master Reference Data
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The SearchFilter model module.
 * @module model/SearchFilter
 * @version 1.0.0
 */
class SearchFilter {
    /**
     * Constructs a new <code>SearchFilter</code>.
     * An indexed Search filter. operator and condition are required. keyword, description or both may be supplied. Filter 0 has the additional runtime requirement that keyword must be present.
     * @alias module:model/SearchFilter
     * @param operator {module:model/SearchFilter.OperatorEnum} Comparison operator used for searchable fields in this filter.
     * @param condition {module:model/SearchFilter.ConditionEnum} Logical condition connecting this filter to another indexed filter. NOT is converted internally to AND NOT. The final filter should normally use an empty condition.
     */
    constructor(operator, condition) { 
        
        SearchFilter.initialize(this, operator, condition);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, operator, condition) { 
        obj['operator'] = operator;
        obj['condition'] = condition;
    }

    /**
     * Constructs a <code>SearchFilter</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SearchFilter} obj Optional instance to populate.
     * @return {module:model/SearchFilter} The populated <code>SearchFilter</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SearchFilter();

            if (data.hasOwnProperty('keyword')) {
                obj['keyword'] = ApiClient.convertToType(data['keyword'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('operator')) {
                obj['operator'] = ApiClient.convertToType(data['operator'], 'String');
            }
            if (data.hasOwnProperty('condition')) {
                obj['condition'] = ApiClient.convertToType(data['condition'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SearchFilter</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SearchFilter</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SearchFilter.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['keyword'] && !(typeof data['keyword'] === 'string' || data['keyword'] instanceof String)) {
            throw new Error("Expected the field `keyword` to be a primitive type in the JSON string but got " + data['keyword']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['operator'] && !(typeof data['operator'] === 'string' || data['operator'] instanceof String)) {
            throw new Error("Expected the field `operator` to be a primitive type in the JSON string but got " + data['operator']);
        }
        // ensure the json data is a string
        if (data['condition'] && !(typeof data['condition'] === 'string' || data['condition'] instanceof String)) {
            throw new Error("Expected the field `condition` to be a primitive type in the JSON string but got " + data['condition']);
        }

        return true;
    }


}

SearchFilter.RequiredProperties = ["operator", "condition"];

/**
 * Optional keyword value to search. A non-empty value may contain letters, numbers, commas, spaces and hyphens and cannot consist only of whitespace.
 * @member {String} keyword
 */
SearchFilter.prototype['keyword'] = undefined;

/**
 * Optional page-description value to search. A non-empty value may contain letters, numbers, commas, spaces and hyphens and cannot consist only of whitespace.
 * @member {String} description
 */
SearchFilter.prototype['description'] = undefined;

/**
 * Comparison operator used for searchable fields in this filter.
 * @member {module:model/SearchFilter.OperatorEnum} operator
 */
SearchFilter.prototype['operator'] = undefined;

/**
 * Logical condition connecting this filter to another indexed filter. NOT is converted internally to AND NOT. The final filter should normally use an empty condition.
 * @member {module:model/SearchFilter.ConditionEnum} condition
 */
SearchFilter.prototype['condition'] = undefined;





/**
 * Allowed values for the <code>operator</code> property.
 * @enum {String}
 * @readonly
 */
SearchFilter['OperatorEnum'] = {

    /**
     * value: "="
     * @const
     */
    "EQUAL": "=",

    /**
     * value: "LIKE"
     * @const
     */
    "LIKE": "LIKE",

    /**
     * value: "unknown_default_open_api"
     * @const
     */
    "unknown_default_open_api": "unknown_default_open_api"
};


/**
 * Allowed values for the <code>condition</code> property.
 * @enum {String}
 * @readonly
 */
SearchFilter['ConditionEnum'] = {

    /**
     * value: ""
     * @const
     */
    "empty": "",

    /**
     * value: "AND"
     * @const
     */
    "AND": "AND",

    /**
     * value: "OR"
     * @const
     */
    "OR": "OR",

    /**
     * value: "NOT"
     * @const
     */
    "NOT": "NOT",

    /**
     * value: "AND NOT"
     * @const
     */
    "AND NOT": "AND NOT",

    /**
     * value: "unknown_default_open_api"
     * @const
     */
    "unknown_default_open_api": "unknown_default_open_api"
};



export default SearchFilter;

