/**
 * MRD API
 * API for retrieving Master Reference Data
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AlissServiceArea model module.
 * @module model/AlissServiceArea
 * @version 1.0.0
 */
class AlissServiceArea {
    /**
     * Constructs a new <code>AlissServiceArea</code>.
     * @alias module:model/AlissServiceArea
     */
    constructor() { 
        
        AlissServiceArea.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>AlissServiceArea</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AlissServiceArea} obj Optional instance to populate.
     * @return {module:model/AlissServiceArea} The populated <code>AlissServiceArea</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AlissServiceArea();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('code')) {
                obj['code'] = ApiClient.convertToType(data['code'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('geojson')) {
                obj['geojson'] = ApiClient.convertToType(data['geojson'], Object);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AlissServiceArea</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AlissServiceArea</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['code'] && !(typeof data['code'] === 'string' || data['code'] instanceof String)) {
            throw new Error("Expected the field `code` to be a primitive type in the JSON string but got " + data['code']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}



/**
 * @member {String} id
 */
AlissServiceArea.prototype['id'] = undefined;

/**
 * @member {String} code
 */
AlissServiceArea.prototype['code'] = undefined;

/**
 * @member {String} name
 */
AlissServiceArea.prototype['name'] = undefined;

/**
 * @member {String} type
 */
AlissServiceArea.prototype['type'] = undefined;

/**
 * GeoJSON object included when requested.
 * @member {Object} geojson
 */
AlissServiceArea.prototype['geojson'] = undefined;






export default AlissServiceArea;

