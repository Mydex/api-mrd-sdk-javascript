/**
 * MRD API
 * API for retrieving Master Reference Data
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import SearchPagination from './SearchPagination';
import SearchResultItem from './SearchResultItem';

/**
 * The PaginatedSearchResponse model module.
 * @module model/PaginatedSearchResponse
 * @version 1.0.0
 */
class PaginatedSearchResponse {
    /**
     * Constructs a new <code>PaginatedSearchResponse</code>.
     * Successful paginated search response returned when the all query parameter is absent.
     * @alias module:model/PaginatedSearchResponse
     * @param count {String} Total number of database rows matching the search. PDO currently returns this count as a string.
     * @param data {Array.<module:model/SearchResultItem>} Matching MRD search records.
     * @param pagination {module:model/SearchPagination} 
     */
    constructor(count, data, pagination) { 
        
        PaginatedSearchResponse.initialize(this, count, data, pagination);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, count, data, pagination) { 
        obj['count'] = count;
        obj['data'] = data;
        obj['pagination'] = pagination;
    }

    /**
     * Constructs a <code>PaginatedSearchResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PaginatedSearchResponse} obj Optional instance to populate.
     * @return {module:model/PaginatedSearchResponse} The populated <code>PaginatedSearchResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PaginatedSearchResponse();

            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'String');
            }
            if (data.hasOwnProperty('data')) {
                obj['data'] = ApiClient.convertToType(data['data'], [SearchResultItem]);
            }
            if (data.hasOwnProperty('pagination')) {
                obj['pagination'] = SearchPagination.constructFromObject(data['pagination']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PaginatedSearchResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PaginatedSearchResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PaginatedSearchResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['count'] && !(typeof data['count'] === 'string' || data['count'] instanceof String)) {
            throw new Error("Expected the field `count` to be a primitive type in the JSON string but got " + data['count']);
        }
        if (data['data']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['data'])) {
                throw new Error("Expected the field `data` to be an array in the JSON data but got " + data['data']);
            }
            // validate the optional field `data` (array)
            for (const item of data['data']) {
                SearchResultItem.validateJSON(item);
            };
        }
        // validate the optional field `pagination`
        if (data['pagination']) { // data not null
          SearchPagination.validateJSON(data['pagination']);
        }

        return true;
    }


}

PaginatedSearchResponse.RequiredProperties = ["count", "data", "pagination"];

/**
 * Total number of database rows matching the search. PDO currently returns this count as a string.
 * @member {String} count
 */
PaginatedSearchResponse.prototype['count'] = undefined;

/**
 * Matching MRD search records.
 * @member {Array.<module:model/SearchResultItem>} data
 */
PaginatedSearchResponse.prototype['data'] = undefined;

/**
 * @member {module:model/SearchPagination} pagination
 */
PaginatedSearchResponse.prototype['pagination'] = undefined;






export default PaginatedSearchResponse;

