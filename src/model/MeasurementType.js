/**
 * MRD API
 * API for retrieving Master Reference Data
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The MeasurementType model module.
 * @module model/MeasurementType
 * @version 1.0.0
 */
class MeasurementType {
    /**
     * Constructs a new <code>MeasurementType</code>.
     * Measurement definition including its group and unit.
     * @alias module:model/MeasurementType
     * @param measurementId {String} Measurement types.
     * @param measurementName {String} 
     * @param measurementGroup {String} 
     * @param measurementUnit {String} 
     */
    constructor(measurementId, measurementName, measurementGroup, measurementUnit) { 
        
        MeasurementType.initialize(this, measurementId, measurementName, measurementGroup, measurementUnit);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, measurementId, measurementName, measurementGroup, measurementUnit) { 
        obj['measurement_id'] = measurementId;
        obj['measurement_name'] = measurementName;
        obj['measurement_group'] = measurementGroup;
        obj['measurement_unit'] = measurementUnit;
    }

    /**
     * Constructs a <code>MeasurementType</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/MeasurementType} obj Optional instance to populate.
     * @return {module:model/MeasurementType} The populated <code>MeasurementType</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new MeasurementType();

            if (data.hasOwnProperty('measurement_id')) {
                obj['measurement_id'] = ApiClient.convertToType(data['measurement_id'], 'String');
            }
            if (data.hasOwnProperty('measurement_name')) {
                obj['measurement_name'] = ApiClient.convertToType(data['measurement_name'], 'String');
            }
            if (data.hasOwnProperty('measurement_group')) {
                obj['measurement_group'] = ApiClient.convertToType(data['measurement_group'], 'String');
            }
            if (data.hasOwnProperty('measurement_unit')) {
                obj['measurement_unit'] = ApiClient.convertToType(data['measurement_unit'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>MeasurementType</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>MeasurementType</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of MeasurementType.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['measurement_id'] && !(typeof data['measurement_id'] === 'string' || data['measurement_id'] instanceof String)) {
            throw new Error("Expected the field `measurement_id` to be a primitive type in the JSON string but got " + data['measurement_id']);
        }
        // ensure the json data is a string
        if (data['measurement_name'] && !(typeof data['measurement_name'] === 'string' || data['measurement_name'] instanceof String)) {
            throw new Error("Expected the field `measurement_name` to be a primitive type in the JSON string but got " + data['measurement_name']);
        }
        // ensure the json data is a string
        if (data['measurement_group'] && !(typeof data['measurement_group'] === 'string' || data['measurement_group'] instanceof String)) {
            throw new Error("Expected the field `measurement_group` to be a primitive type in the JSON string but got " + data['measurement_group']);
        }
        // ensure the json data is a string
        if (data['measurement_unit'] && !(typeof data['measurement_unit'] === 'string' || data['measurement_unit'] instanceof String)) {
            throw new Error("Expected the field `measurement_unit` to be a primitive type in the JSON string but got " + data['measurement_unit']);
        }

        return true;
    }


}

MeasurementType.RequiredProperties = ["measurement_id", "measurement_name", "measurement_group", "measurement_unit"];

/**
 * Measurement types.
 * @member {String} measurement_id
 */
MeasurementType.prototype['measurement_id'] = undefined;

/**
 * @member {String} measurement_name
 */
MeasurementType.prototype['measurement_name'] = undefined;

/**
 * @member {String} measurement_group
 */
MeasurementType.prototype['measurement_group'] = undefined;

/**
 * @member {String} measurement_unit
 */
MeasurementType.prototype['measurement_unit'] = undefined;






export default MeasurementType;

