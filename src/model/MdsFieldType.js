/**
 * MRD API
 * API for retrieving Master Reference Data
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The MdsFieldType model module.
 * @module model/MdsFieldType
 * @version 1.0.0
 */
class MdsFieldType {
    /**
     * Constructs a new <code>MdsFieldType</code>.
     * Basic MDS field information.
     * @alias module:model/MdsFieldType
     * @param fieldName {String} 
     * @param displayName {String} 
     * @param dataType {String} 
     */
    constructor(fieldName, displayName, dataType) { 
        
        MdsFieldType.initialize(this, fieldName, displayName, dataType);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, fieldName, displayName, dataType) { 
        obj['field_name'] = fieldName;
        obj['display_name'] = displayName;
        obj['data_type'] = dataType;
    }

    /**
     * Constructs a <code>MdsFieldType</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/MdsFieldType} obj Optional instance to populate.
     * @return {module:model/MdsFieldType} The populated <code>MdsFieldType</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new MdsFieldType();

            if (data.hasOwnProperty('field_name')) {
                obj['field_name'] = ApiClient.convertToType(data['field_name'], 'String');
            }
            if (data.hasOwnProperty('display_name')) {
                obj['display_name'] = ApiClient.convertToType(data['display_name'], 'String');
            }
            if (data.hasOwnProperty('data_type')) {
                obj['data_type'] = ApiClient.convertToType(data['data_type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>MdsFieldType</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>MdsFieldType</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of MdsFieldType.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['field_name'] && !(typeof data['field_name'] === 'string' || data['field_name'] instanceof String)) {
            throw new Error("Expected the field `field_name` to be a primitive type in the JSON string but got " + data['field_name']);
        }
        // ensure the json data is a string
        if (data['display_name'] && !(typeof data['display_name'] === 'string' || data['display_name'] instanceof String)) {
            throw new Error("Expected the field `display_name` to be a primitive type in the JSON string but got " + data['display_name']);
        }
        // ensure the json data is a string
        if (data['data_type'] && !(typeof data['data_type'] === 'string' || data['data_type'] instanceof String)) {
            throw new Error("Expected the field `data_type` to be a primitive type in the JSON string but got " + data['data_type']);
        }

        return true;
    }


}

MdsFieldType.RequiredProperties = ["field_name", "display_name", "data_type"];

/**
 * @member {String} field_name
 */
MdsFieldType.prototype['field_name'] = undefined;

/**
 * @member {String} display_name
 */
MdsFieldType.prototype['display_name'] = undefined;

/**
 * @member {String} data_type
 */
MdsFieldType.prototype['data_type'] = undefined;






export default MdsFieldType;

