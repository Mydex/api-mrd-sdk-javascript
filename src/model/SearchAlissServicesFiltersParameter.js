/**
 * MRD API
 * API for retrieving Master Reference Data
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AlissAccessibilityFeaturesFilter from './AlissAccessibilityFeaturesFilter';
import AlissCategoriesFilter from './AlissCategoriesFilter';
import AlissCommunityGroupsFilter from './AlissCommunityGroupsFilter';
import AlissLocationsFilter from './AlissLocationsFilter';
import AlissOrganisationsFilter from './AlissOrganisationsFilter';
import AlissServiceAreasFilter from './AlissServiceAreasFilter';
import AlissServicesFilter from './AlissServicesFilter';

/**
 * The SearchAlissServicesFiltersParameter model module.
 * @module model/SearchAlissServicesFiltersParameter
 * @version 1.0.0
 */
class SearchAlissServicesFiltersParameter {
    /**
     * Constructs a new <code>SearchAlissServicesFiltersParameter</code>.
     * @alias module:model/SearchAlissServicesFiltersParameter
     */
    constructor() { 
        
        SearchAlissServicesFiltersParameter.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>SearchAlissServicesFiltersParameter</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SearchAlissServicesFiltersParameter} obj Optional instance to populate.
     * @return {module:model/SearchAlissServicesFiltersParameter} The populated <code>SearchAlissServicesFiltersParameter</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SearchAlissServicesFiltersParameter();

            if (data.hasOwnProperty('services')) {
                obj['services'] = ApiClient.convertToType(data['services'], [AlissServicesFilter]);
            }
            if (data.hasOwnProperty('organisations')) {
                obj['organisations'] = ApiClient.convertToType(data['organisations'], [AlissOrganisationsFilter]);
            }
            if (data.hasOwnProperty('categories')) {
                obj['categories'] = ApiClient.convertToType(data['categories'], [AlissCategoriesFilter]);
            }
            if (data.hasOwnProperty('service_areas')) {
                obj['service_areas'] = ApiClient.convertToType(data['service_areas'], [AlissServiceAreasFilter]);
            }
            if (data.hasOwnProperty('locations')) {
                obj['locations'] = ApiClient.convertToType(data['locations'], [AlissLocationsFilter]);
            }
            if (data.hasOwnProperty('accessibility_features')) {
                obj['accessibility_features'] = ApiClient.convertToType(data['accessibility_features'], [AlissAccessibilityFeaturesFilter]);
            }
            if (data.hasOwnProperty('community_groups')) {
                obj['community_groups'] = ApiClient.convertToType(data['community_groups'], [AlissCommunityGroupsFilter]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SearchAlissServicesFiltersParameter</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SearchAlissServicesFiltersParameter</code>.
     */
    static validateJSON(data) {
        if (data['services']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['services'])) {
                throw new Error("Expected the field `services` to be an array in the JSON data but got " + data['services']);
            }
            // validate the optional field `services` (array)
            for (const item of data['services']) {
                AlissServicesFilter.validateJSON(item);
            };
        }
        if (data['organisations']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['organisations'])) {
                throw new Error("Expected the field `organisations` to be an array in the JSON data but got " + data['organisations']);
            }
            // validate the optional field `organisations` (array)
            for (const item of data['organisations']) {
                AlissOrganisationsFilter.validateJSON(item);
            };
        }
        if (data['categories']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['categories'])) {
                throw new Error("Expected the field `categories` to be an array in the JSON data but got " + data['categories']);
            }
            // validate the optional field `categories` (array)
            for (const item of data['categories']) {
                AlissCategoriesFilter.validateJSON(item);
            };
        }
        if (data['service_areas']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['service_areas'])) {
                throw new Error("Expected the field `service_areas` to be an array in the JSON data but got " + data['service_areas']);
            }
            // validate the optional field `service_areas` (array)
            for (const item of data['service_areas']) {
                AlissServiceAreasFilter.validateJSON(item);
            };
        }
        if (data['locations']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['locations'])) {
                throw new Error("Expected the field `locations` to be an array in the JSON data but got " + data['locations']);
            }
            // validate the optional field `locations` (array)
            for (const item of data['locations']) {
                AlissLocationsFilter.validateJSON(item);
            };
        }
        if (data['accessibility_features']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['accessibility_features'])) {
                throw new Error("Expected the field `accessibility_features` to be an array in the JSON data but got " + data['accessibility_features']);
            }
            // validate the optional field `accessibility_features` (array)
            for (const item of data['accessibility_features']) {
                AlissAccessibilityFeaturesFilter.validateJSON(item);
            };
        }
        if (data['community_groups']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['community_groups'])) {
                throw new Error("Expected the field `community_groups` to be an array in the JSON data but got " + data['community_groups']);
            }
            // validate the optional field `community_groups` (array)
            for (const item of data['community_groups']) {
                AlissCommunityGroupsFilter.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * @member {Array.<module:model/AlissServicesFilter>} services
 */
SearchAlissServicesFiltersParameter.prototype['services'] = undefined;

/**
 * @member {Array.<module:model/AlissOrganisationsFilter>} organisations
 */
SearchAlissServicesFiltersParameter.prototype['organisations'] = undefined;

/**
 * @member {Array.<module:model/AlissCategoriesFilter>} categories
 */
SearchAlissServicesFiltersParameter.prototype['categories'] = undefined;

/**
 * @member {Array.<module:model/AlissServiceAreasFilter>} service_areas
 */
SearchAlissServicesFiltersParameter.prototype['service_areas'] = undefined;

/**
 * @member {Array.<module:model/AlissLocationsFilter>} locations
 */
SearchAlissServicesFiltersParameter.prototype['locations'] = undefined;

/**
 * @member {Array.<module:model/AlissAccessibilityFeaturesFilter>} accessibility_features
 */
SearchAlissServicesFiltersParameter.prototype['accessibility_features'] = undefined;

/**
 * @member {Array.<module:model/AlissCommunityGroupsFilter>} community_groups
 */
SearchAlissServicesFiltersParameter.prototype['community_groups'] = undefined;






export default SearchAlissServicesFiltersParameter;

