/**
 * MRD API
 * API for retrieving Master Reference Data
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The MeasurementBodyPosition model module.
 * @module model/MeasurementBodyPosition
 * @version 1.0.0
 */
class MeasurementBodyPosition {
    /**
     * Constructs a new <code>MeasurementBodyPosition</code>.
     * Body position lookup record.
     * @alias module:model/MeasurementBodyPosition
     * @param id {String} 
     * @param bodyPositionId {String} 
     * @param description {String} 
     */
    constructor(id, bodyPositionId, description) { 
        
        MeasurementBodyPosition.initialize(this, id, bodyPositionId, description);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, bodyPositionId, description) { 
        obj['id'] = id;
        obj['body_position_id'] = bodyPositionId;
        obj['description'] = description;
    }

    /**
     * Constructs a <code>MeasurementBodyPosition</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/MeasurementBodyPosition} obj Optional instance to populate.
     * @return {module:model/MeasurementBodyPosition} The populated <code>MeasurementBodyPosition</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new MeasurementBodyPosition();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('body_position_id')) {
                obj['body_position_id'] = ApiClient.convertToType(data['body_position_id'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>MeasurementBodyPosition</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>MeasurementBodyPosition</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of MeasurementBodyPosition.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['body_position_id'] && !(typeof data['body_position_id'] === 'string' || data['body_position_id'] instanceof String)) {
            throw new Error("Expected the field `body_position_id` to be a primitive type in the JSON string but got " + data['body_position_id']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }

        return true;
    }


}

MeasurementBodyPosition.RequiredProperties = ["id", "body_position_id", "description"];

/**
 * @member {String} id
 */
MeasurementBodyPosition.prototype['id'] = undefined;

/**
 * @member {String} body_position_id
 */
MeasurementBodyPosition.prototype['body_position_id'] = undefined;

/**
 * @member {String} description
 */
MeasurementBodyPosition.prototype['description'] = undefined;






export default MeasurementBodyPosition;

