/**
 * MRD API
 * API for retrieving Master Reference Data
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import SearchResultItem from './SearchResultItem';

/**
 * The UnpaginatedSearchResponse model module.
 * @module model/UnpaginatedSearchResponse
 * @version 1.0.0
 */
class UnpaginatedSearchResponse {
    /**
     * Constructs a new <code>UnpaginatedSearchResponse</code>.
     * Successful unpaginated search response returned when the all query parameter is present.
     * @alias module:model/UnpaginatedSearchResponse
     * @param count {String} Total number of database rows matching the search. PDO currently returns this count as a string.
     * @param data {Array.<module:model/SearchResultItem>} All matching MRD search records.
     */
    constructor(count, data) { 
        
        UnpaginatedSearchResponse.initialize(this, count, data);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, count, data) { 
        obj['count'] = count;
        obj['data'] = data;
    }

    /**
     * Constructs a <code>UnpaginatedSearchResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UnpaginatedSearchResponse} obj Optional instance to populate.
     * @return {module:model/UnpaginatedSearchResponse} The populated <code>UnpaginatedSearchResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UnpaginatedSearchResponse();

            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'String');
            }
            if (data.hasOwnProperty('data')) {
                obj['data'] = ApiClient.convertToType(data['data'], [SearchResultItem]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UnpaginatedSearchResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UnpaginatedSearchResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of UnpaginatedSearchResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['count'] && !(typeof data['count'] === 'string' || data['count'] instanceof String)) {
            throw new Error("Expected the field `count` to be a primitive type in the JSON string but got " + data['count']);
        }
        if (data['data']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['data'])) {
                throw new Error("Expected the field `data` to be an array in the JSON data but got " + data['data']);
            }
            // validate the optional field `data` (array)
            for (const item of data['data']) {
                SearchResultItem.validateJSON(item);
            };
        }

        return true;
    }


}

UnpaginatedSearchResponse.RequiredProperties = ["count", "data"];

/**
 * Total number of database rows matching the search. PDO currently returns this count as a string.
 * @member {String} count
 */
UnpaginatedSearchResponse.prototype['count'] = undefined;

/**
 * All matching MRD search records.
 * @member {Array.<module:model/SearchResultItem>} data
 */
UnpaginatedSearchResponse.prototype['data'] = undefined;






export default UnpaginatedSearchResponse;

