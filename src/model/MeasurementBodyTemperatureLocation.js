/**
 * MRD API
 * API for retrieving Master Reference Data
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The MeasurementBodyTemperatureLocation model module.
 * @module model/MeasurementBodyTemperatureLocation
 * @version 1.0.0
 */
class MeasurementBodyTemperatureLocation {
    /**
     * Constructs a new <code>MeasurementBodyTemperatureLocation</code>.
     * Body temperature location lookup record.
     * @alias module:model/MeasurementBodyTemperatureLocation
     * @param id {String} 
     * @param bodyTemperatureLocationId {String} 
     * @param description {String} 
     */
    constructor(id, bodyTemperatureLocationId, description) { 
        
        MeasurementBodyTemperatureLocation.initialize(this, id, bodyTemperatureLocationId, description);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, bodyTemperatureLocationId, description) { 
        obj['id'] = id;
        obj['body_temperature_location_id'] = bodyTemperatureLocationId;
        obj['description'] = description;
    }

    /**
     * Constructs a <code>MeasurementBodyTemperatureLocation</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/MeasurementBodyTemperatureLocation} obj Optional instance to populate.
     * @return {module:model/MeasurementBodyTemperatureLocation} The populated <code>MeasurementBodyTemperatureLocation</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new MeasurementBodyTemperatureLocation();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('body_temperature_location_id')) {
                obj['body_temperature_location_id'] = ApiClient.convertToType(data['body_temperature_location_id'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>MeasurementBodyTemperatureLocation</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>MeasurementBodyTemperatureLocation</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of MeasurementBodyTemperatureLocation.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['body_temperature_location_id'] && !(typeof data['body_temperature_location_id'] === 'string' || data['body_temperature_location_id'] instanceof String)) {
            throw new Error("Expected the field `body_temperature_location_id` to be a primitive type in the JSON string but got " + data['body_temperature_location_id']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }

        return true;
    }


}

MeasurementBodyTemperatureLocation.RequiredProperties = ["id", "body_temperature_location_id", "description"];

/**
 * @member {String} id
 */
MeasurementBodyTemperatureLocation.prototype['id'] = undefined;

/**
 * @member {String} body_temperature_location_id
 */
MeasurementBodyTemperatureLocation.prototype['body_temperature_location_id'] = undefined;

/**
 * @member {String} description
 */
MeasurementBodyTemperatureLocation.prototype['description'] = undefined;






export default MeasurementBodyTemperatureLocation;

