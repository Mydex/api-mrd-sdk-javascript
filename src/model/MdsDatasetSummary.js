/**
 * MRD API
 * API for retrieving Master Reference Data
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The MdsDatasetSummary model module.
 * @module model/MdsDatasetSummary
 * @version 1.0.0
 */
class MdsDatasetSummary {
    /**
     * Constructs a new <code>MdsDatasetSummary</code>.
     * Summary information for a published MDS dataset.
     * @alias module:model/MdsDatasetSummary
     * @param datasetMachineName {String} 
     * @param datasetName {String} 
     * @param status {String} 
     */
    constructor(datasetMachineName, datasetName, status) { 
        
        MdsDatasetSummary.initialize(this, datasetMachineName, datasetName, status);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, datasetMachineName, datasetName, status) { 
        obj['dataset_machine_name'] = datasetMachineName;
        obj['dataset_name'] = datasetName;
        obj['status'] = status;
    }

    /**
     * Constructs a <code>MdsDatasetSummary</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/MdsDatasetSummary} obj Optional instance to populate.
     * @return {module:model/MdsDatasetSummary} The populated <code>MdsDatasetSummary</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new MdsDatasetSummary();

            if (data.hasOwnProperty('dataset_machine_name')) {
                obj['dataset_machine_name'] = ApiClient.convertToType(data['dataset_machine_name'], 'String');
            }
            if (data.hasOwnProperty('dataset_name')) {
                obj['dataset_name'] = ApiClient.convertToType(data['dataset_name'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>MdsDatasetSummary</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>MdsDatasetSummary</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of MdsDatasetSummary.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['dataset_machine_name'] && !(typeof data['dataset_machine_name'] === 'string' || data['dataset_machine_name'] instanceof String)) {
            throw new Error("Expected the field `dataset_machine_name` to be a primitive type in the JSON string but got " + data['dataset_machine_name']);
        }
        // ensure the json data is a string
        if (data['dataset_name'] && !(typeof data['dataset_name'] === 'string' || data['dataset_name'] instanceof String)) {
            throw new Error("Expected the field `dataset_name` to be a primitive type in the JSON string but got " + data['dataset_name']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }

        return true;
    }


}

MdsDatasetSummary.RequiredProperties = ["dataset_machine_name", "dataset_name", "status"];

/**
 * @member {String} dataset_machine_name
 */
MdsDatasetSummary.prototype['dataset_machine_name'] = undefined;

/**
 * @member {String} dataset_name
 */
MdsDatasetSummary.prototype['dataset_name'] = undefined;

/**
 * @member {String} status
 */
MdsDatasetSummary.prototype['status'] = undefined;






export default MdsDatasetSummary;

